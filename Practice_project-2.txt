							Order Statistics
DESCRIPTION

This section will guide you to: 

● Write a program in Java to find the fourth smallest element in an unsorted list 
● Use Eclipse (the popular text editor for Java programs) 
● Push code to Git

package arrays;

public class Smallest 
{

	public static void main(String[] args) 
	{
		// TODO Auto-generated method stub
		KSmallest obj = new KSmallest();
		int arr[] = {12,23,34,45,56,67,78};
		int n = arr.length;
		int k = 4;
		System.out.println("4th Smallest Number in array : "+ obj.kthSmallest(arr, 0, n-1, k));
	}

}
class KSmallest
{
	int kthSmallest(int arr[], int l, int r, int k) 
	{ 
		if (k > 0 && k <= r - l + 1) 
    	{ 
        			int pos = randomPartition(arr, l, r); 
        			if (pos-l == k-1) 
        				return arr[pos]; 
        			if (pos-l > k-1) 
            			return kthSmallest(arr, l, pos-1, k); 
        			return kthSmallest(arr, pos+1, r, k-pos+l-1); 
    	} 
        return Integer.MAX_VALUE; 
    } 
	void swap(int arr[], int i, int j) 
    { 
        int temp = arr[i]; 
        arr[i] = arr[j]; 
        arr[j] = temp; 
    } 
    int partition(int arr[], int l, int r) 
    { 
        int x = arr[r], i = l; 
        for (int j = l; j <= r - 1; j++) 
        { 
            if (arr[j] <= x) 
            { 
                swap(arr, i, j); 
                i++; 
            } 
        } 
        swap(arr, i, r); 
        return i; 
    } 
    int randomPartition(int arr[], int l, int r) 
    { 
        int n = r-l+1; 
        int pivot = (int)(Math.random()) * (n-1); 
        swap(arr, l + pivot, r); 
        return partition(arr, l, r); 
    } 
}

OUTPUT:

4th Smallest Number in array : 45

