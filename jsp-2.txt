					JSP Implicit Objects 
DESCRIPTION
This section will guide you to:
● Create a JSP file to test JSP implicit objects and run it in the browser
● Do error handling in a separate JSP file
● Show how JSP redirection is done to another JSP file

implicit-object-demo.jsp

<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8" errorPage="error.jsp"%>

<h3>Implicit Objects Demo</h3>

<%
	String responseCheck = request.getParameter("office");

	// if office parameter was set, tthen redirect the user to response-redirect.jsp
	if (responseCheck != null) {
		response.setStatus(response.SC_MOVED_TEMPORARILY); // HTTP status code 302
		response.setHeader("Location", "response-redirect.jsp?office=" + responseCheck);
	}

	// Error handling
	String errorCheck = request.getParameter("error");
	//simulate throw error
	if (errorCheck != null) {
		int x = 0;
		if (x == 0)
			throw new RuntimeException("Exception has been raised");
	}
%>

<%
	int serverPort = request.getServerPort();
	out.println("The Server is running on port " + String.valueOf(serverPort) + "<br>");

	out.println("Servlet Name is " + config.getServletName() + "<br>");

	out.println("Server Info:" + application.getServerInfo() + "<br>");

	String pageName = page.toString();
	out.println("The name of the page is " + pageName + "<br>");

	pageContext.setAttribute("userid", "John Doe");
	out.println("userId attribute from pageContext: " + pageContext.getAttribute("userid") + "<br>");
%>

response-redirect.jsp

<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<h3> Inside response-redirect.jsp</h3>

 <%
	String office = request.getParameter("office");
 
	if (office != null)
		out.println("value of Office obtained :" + office + " <br>");
	else
		out.println("No value of Office found <br>");
%>

error.jsp

<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8" isErrorPage="true"%>

<h2>Oops! Something went wrong. Contact our customer care executive. 9865454332</h2>

OUTPUT

http://localhost:3090/ImplicitObjects/implicit-object-demo.jsp

Implicit Objects Demo

The Server is running on port 3090
Servlet Name is jsp
Server Info:Apache Tomcat/8.0.36
The name of the page is org.apache.jsp.implicit_002dobject_002ddemo_jsp@691957f
userId attribute from pageContext: John Doe

http://localhost:3090/ImplicitObjects/implicit-object-demo.jsp?error=9
Oops! Something went wrong. Contact our customer care executive. 9865454332